-- ~/.config/hypr/conf/layout.lua

hl.config({
	dwindle = {
		preserve_split = true, -- You probably want this
	},

	master = {
		-- master layout options go here if needed
	},

	scrolling = {
		fullscreen_on_one_column = true,
		focus_fit_method = 1,
		column_width = 0.8,
	},

	gestures = {
		-- gesture options go here if needed
	},
})
